---
interface Props {
  header: string;
  subHeader: string;
}
const { header, subHeader } = Astro.props;
---

<div class="page-header">
  <h1 class="header">{header}</h1>
  <h2 class="page-subheader">{subHeader}</h2>
</div>

<style>
  .page-header {
    padding: 60px 24px 80px 0px;
  }
</style>
